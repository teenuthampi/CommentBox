/ Post Index Page
- @posts.each do |post|
  .post_wrapper
    %h2.title= link_to post.title, post
    %p.date= post.created_at.strftime("%B %d, %Y")
    %p= link_to 'View', post_path(post.id),class: "button"
